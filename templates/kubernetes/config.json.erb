{
    "KOPS_STATE_STORE": "<%= puts "KOPS_STATE_STORE:"; STDIN.gets.chomp %>",
    "CLUSTER_TLD": "<%= puts "CLUSTER_TLD:"; STDIN.gets.chomp %>",
    "CLUSTER_TLD_ROUTE53_ZONE_ID": "<%= puts "CLUSTER_TLD_ROUTE53_ZONE_ID:"; STDIN.gets.chomp %>",
    "AWS_PROFILE": "<%= KubernetesSetup.aws_profile %>",
    "<%= KubernetesSetup.cluster_name %>": {
        "AMI": "GENERATED_AMI",
        "CLUSTER_MASTER_ZONES": "<%= puts "CLUSTER_MASTER_ZONES:"; zones = STDIN.gets.chomp %>",
        "CLUSTER_ZONES": "<%= zones %>",
        "CLUSTER_MASTER_SIZE": "<%= puts "CLUSTER_MASTER_SIZE:"; size = STDIN.gets.chomp %>",
        "CLUSTER_NODE_SIZE": "<%= size %>",
        "CIDR": "<%= puts "CIDR:"; STDIN.gets.chomp %>"
    }
}
